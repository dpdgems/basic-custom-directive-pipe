import { NumberDecimalDirective } from './number-decimal.directive';

describe('NumberDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
